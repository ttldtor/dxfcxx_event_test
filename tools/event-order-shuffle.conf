# Copyright (c) 2026 ttldtor.
# SPDX-License-Identifier: BSL-1.0

# Distribute the event-block position deterministically across publications.
EXPERIMENT_TITLE=Shuffled event-type order control
EXPERIMENT_OBJECTIVE=Determine whether deterministic per-publication shuffling distributes FEED listener deficit between event types without reducing total deficit.
EXPERIMENT_VARIABLE=Deterministically shuffled event-type block order compared with the related fixed-order control.
EXPERIMENT_CONTROLS=Compiler, host, FEED role, 375 symbols, 150,000 recurring events/s, cadence, random seed, warm-up, and duration.
EXPERIMENT_SUCCESS_CRITERIA=Compare per-type and total listener deficit, generator cost, callback shape, latency, QD drops, buffers, I/O rates, and CPU.
EXPERIMENT_LIMITATIONS=Shuffling is a diagnostic control rather than a product workaround and cannot identify the internal supersession stage.
REPETITIONS=3
WARMUP=30s
DURATION=1m
WINDOW=10s
BATCH_TIMEOUT=30s
STARTUP_TIMEOUT=30s
MONITORING_PERIOD=10s
CLIENT_ROLE=feed
COOLDOWN_SECONDS=10
ADDRESS=127.0.0.1:7400
LISTEN_ADDRESS=:7400

PROFILE=order-shuffled|SUB:Q375;T375;E375;S375@10ms#375~22805
